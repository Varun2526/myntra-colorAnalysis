# Base URL of the FastAPI backend
VITE_API_BASE_URL=http://localhost:8000

# Set to "true" to use the bundled mock analysis result instead of the
# backend (useful for UI work without running FastAPI)
VITE_USE_MOCK_ANALYSIS=false
